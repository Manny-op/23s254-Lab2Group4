#include <iostream>
#include <string.h>
class read_names {

    string names [4];
    // pull the names from the readme file and then save it into an array
    read_names();

    // return the names to main
    get_names() {

        return names[];

    }


}